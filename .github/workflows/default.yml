---
name: Default

on:
  workflow_dispatch:
  push:
    paths:
      - "cmd/**"
      - "internal/**"
      - "Makefile"
      - "go.mod"
      - "go.sum"
  pull_request:
    paths:
      - "cmd/**"
      - "internal/**"
      - "Makefile"
      - "go.mod"
      - "go.sum"

jobs:
  test:
    runs-on: ubuntu-22.04
    container: cherts/pgscv-test-runner:1.0.5

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Prepare test environment
        run: prepare-test-environment.sh
      - name: Run test
        run: make test
